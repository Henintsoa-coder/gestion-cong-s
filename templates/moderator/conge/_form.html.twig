<div>
    <h4 class="mt-5 mb-5">
    <strong>
        {{ user.nom }} 
        {{ user.prenom }}
    </strong>
</h4>
</div>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
